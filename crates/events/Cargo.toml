[package]
name = "events"
version = "0.0.1"
edition = "2024"

[lints]
workspace = true

[dependencies]
chrono = { version = "0.4", default-features = false, features = ["clock", "std"] }
log = "0.4"
state = { path = "../state" }
